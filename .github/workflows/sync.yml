name: Job2Calendar Sync

on:
  schedule:
    # Runs once a day at midnight UTC.
    - cron: '0 0 * * *'
  # Allows manual triggering from the GitHub Actions tab
  workflow_dispatch: 

jobs:
  sync-jobs:
    runs-on: ubuntu-latest
    
    # Grant permissions needed to push the updated files back to the repo
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Job2Calendar sync
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python main.py

      - name: Commit and push updates
        # Still commit whatever the sync managed to write when it reported a
        # partial failure, while the job itself stays red.
        if: ${{ !cancelled() }}
        run: |
          set -euo pipefail
          
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # Add the database file (in data/) and the calendar feed file (in root)
          git add processed_jobs.json jobs.ics
          
          # Check if there are any staged changes
          if git diff --staged --quiet; then
            echo "No new jobs processed. Nothing to commit."
          else
            git commit -m "chore: update calendar feed and processed jobs [skip ci]"
            git push
          fi


          